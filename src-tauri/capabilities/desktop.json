{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Permissions for desktop-only plugins (updater, process). The updater is not available on mobile, which ships through app stores.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": [
    "main"
  ],
  "permissions": [
    "updater:default"
  ]
}
